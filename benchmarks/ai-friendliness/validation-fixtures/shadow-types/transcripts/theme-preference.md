# Theme preference transcript

Build a settings panel with a light/dark theme toggle that survives reloads and
stays in sync across multiple tabs.

The agent chose `react-mnemonic`, but it also created a local
`react-mnemonic.d.ts` shim instead of using the published package types.
