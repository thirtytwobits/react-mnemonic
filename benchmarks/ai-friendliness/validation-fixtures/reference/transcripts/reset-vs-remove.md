# Reset vs Remove Transcript

- User prompt: Build a preferences screen with both a Reset to defaults action and a Remove saved preference action, and preserve the distinction between them after reload.
- Follow-up prompts: 0
- Outcome: Used `reset()` and `remove()` as distinct operations.
